Back end thinking 

Data needed:
This tool doesnt need any data to function other than words.

Where is it stored:
The data that they type is stored in their localStorage/ on their browser. There is no backend colecting.

Is it temporary or persistent:
The data/ words you put in is only temporary.

Does the system need memory between sessions:
It doesnt need memory between sessions rather it wipes every time you refresh the page.

Does the system require AI inference:
This system doesnt require AI inference as there is no AI in the program.

How many API calls are realistically required:
Right now 0 